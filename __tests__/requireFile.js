const { requireFile } = require('../src');

describe('Single File', () => {
    it.todo('should throw error if file name is not a string');
    it.todo('should throw error on invalid file name');
    it.todo('should throw error when given file does\'nt exist');
    
    describe('On Success', () => {
        it.todo('should return an object');
        it.todo('should have all the functions in the file');
    });
});